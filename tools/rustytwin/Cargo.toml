[package]
name = "rustytwin"
version = "0.1.0"
edition = "2021"
description = "Differential test runner for RustyCpp migrations"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.142"
